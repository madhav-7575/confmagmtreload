# Copy this file to .env and fill in real values.
# app.py works fine with the defaults below for local/demo use.

SECRET_KEY=change-this-to-a-random-secret-string
FLASK_DEBUG=1
PORT=5000

# Optional — only needed if you wire up grok_service.py / AI features
GROQ_API_KEY=your_groq_api_key_here
